package com.xently.holla.data.source.remote

import android.content.Context
import com.xently.holla.data.source.BaseDataSource

abstract class BaseRemoteDataSource internal constructor(context: Context) : BaseDataSource(context)