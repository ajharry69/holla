package com.xently.holla.data.source.local

import android.content.Context
import com.xently.holla.data.source.BaseDataSource

abstract class BaseLocalDataSource internal constructor(context: Context) : BaseDataSource(context)